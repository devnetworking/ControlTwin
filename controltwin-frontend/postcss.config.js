export default {
  plugins: {
    // Tailwind CSS: transforms @tailwind directives into generated utility CSS.
    tailwindcss: {},
    // Autoprefixer: adds vendor prefixes for cross-browser CSS compatibility.
    autoprefixer: {},
  },
};
